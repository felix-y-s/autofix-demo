# 이 레포에서 작업할 때 지킬 규칙

- 수정은 최소 범위로 한다. 증상만 가리는 try/catch 추가는 금지.
- 수정 후 반드시 `npm test`를 실행해 통과를 확인한다.
- 커밋 메시지는 `fix: ` 로 시작하고, 원인을 한 줄로 요약해 적는다.
- package.json의 의존성 버전은 임의로 바꾸지 않는다.

## DB를 쓰는 테스트

- `test/order-repository.test.js`는 실제 postgres에 붙어 돈다.
  `DATABASE_URL` 환경변수가 이미 주입되어 있으므로 따로 설정하지 않는다.
- 스키마는 `db/schema.sql` 하나뿐이다. 테이블 구조를 바꿔야 한다면 이 파일을 고친다.
- 테스트가 DB 연결 오류로 실패하면 코드 문제가 아니므로 SQL을 고치지 말고 그대로 보고한다.
